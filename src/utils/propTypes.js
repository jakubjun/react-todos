import PropTypes from 'prop-types';

const layoutPropTypes = {
  children: PropTypes.node.isRequired,
};

const pagePropTypes = {
  children: PropTypes.node.isRequired,
};

export { pagePropTypes, layoutPropTypes };
