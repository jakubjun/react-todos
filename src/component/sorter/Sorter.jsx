const CLASS_NAME = 'sorter';
export function Sorter() {
  // const [selectedSortOption, sortOptions] = useSorter();
  const sortOptions = [
    ['A->z', 'Z->a'],
    ['new->old', 'old->new'],

  ];
  return (

    <div>
      {}
    </div>
  );
}
